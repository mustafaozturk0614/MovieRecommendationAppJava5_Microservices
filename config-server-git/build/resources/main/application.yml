server:
  port: 8889

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mustafaozturk0614/ConfigServer.git
          default-label: main
          skip-ssl-validation: true
          username: mustafaozturk0614
          password: ${git_password}